# SELECT — account format/comment helpers (N=3)

Targets on `AccountState` (pure / low-DI):

1. `formatDurationShort(ms)`
2. `commentStatusChipClass(status)`
3. `formatTimestamp(value)`

Non-overlapping with open order-label / cancel-return / tracking / coupons slices.

# DeSlop — flag/delete

| Spec | Branch |
|------|--------|
| canFlag | auth; null user; deleted/hidden; own vs other |
| flagComment | early return when !canFlag (no prompt/API) |
| deleteComment | early return when !canDelete; confirm deny skips API |
